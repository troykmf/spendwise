const ownerUserIdFieldName = 'user_id';

const titleFieldName = 'title';

const amountFieldName = 'amount';

const transactionTypeFieldName = 'transaction_type';

const tagFieldName = 'tag';

const whenFieldName = 'when';

const noteFieldName = 'note';
