import 'package:flutter/material.dart';

class NewNudgetTabPage extends StatelessWidget {
  const NewNudgetTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SingleChildScrollView(
      child: Column(
        children: [],
      ),
    );
  }
}
